# Web framework & auth
fastapi==0.115.0
uvicorn==0.31.0
python-multipart==0.0.11
fastapi-jwt[authlib]==0.3.0
bcrypt==4.1.0
cryptography>=42.0

# Database
sqlalchemy==1.4.53
psycopg2-binary==2.9.9

# Agent orchestration + durable PostgreSQL checkpoints (LangGraph)
langgraph>=1.0,<2.0
langgraph-checkpoint-postgres>=3.0,<4.0
psycopg[binary,pool]>=3.2,<4.0

# LLM / embeddings (DashScope via OpenAI-compatible API)
openai>=1.55.3
httpx==0.27.2

# Retrieval
elasticsearch==8.12.0
jieba==0.42.1

# Document parsing (text-layer PDF / DOCX; TXT & MD need no library)
pdfplumber==0.11.0
python-docx==1.1.0

# Utilities
python-dotenv==1.0.1
colorlog==6.8.0
